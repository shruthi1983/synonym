<%= form_with(model: synonym, local: true) do |form| %>
  <% if synonym.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(synonym.errors.count, "error") %> prohibited this synonym from being saved:</h2>

      <ul>
      <% synonym.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :English_word %>
    <%= form.text_area :English_word %>
  </div>

  <div class="field">
    <%= form.label :Kannada_meaning %>
    <%= form.text_area :Kannada_meaning %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
